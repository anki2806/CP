#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back
#define all(v) v.begin(), v.end()
#define ANKIT ios_base::sync_with_stdio(false); cin.tie(NULL);

void solve() {
    ll n, m, h;
    vector<ll> a(n);
    vector<ll> op(n);

    for(int i = 0; i < n; i++) {
        cin>>a[i];
        op[i] = a[i];
    }

    vector<ll[2]> b(n);

    for(int i = 0; i < m; i++) {
        cin>>b[i][0];
        cin>>b[i][1];
    }


}

int main() {
    ANKIT

    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}